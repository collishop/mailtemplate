mixin tile(data)
    a(href=data.link target="_blank")
        img.topImage(src=`assets/img/${data.image}.jpg`)
        wrapper.tile
            center
                img(src=`assets/img/${data.discount}.png`)
                p.centerText= data.categorie1
                    br
                    strong= " "+data.categorie2
                            a(href=link)
                a(href=data.link target="_blank")
                    wrapper.btn.pink
                        p.centerText= data.cta
                            span#arrow ►